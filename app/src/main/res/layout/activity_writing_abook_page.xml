<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF">

    <!-- כותרת: כתיבת ספרים -->
    <ImageView
        android:id="@+id/titleWriting"
        android:layout_width="220dp"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="40dp"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        android:src="@drawable/titel_wirting_book" />

    <!-- כפתור: יצירת ספר חדש -->
    <ImageButton
        android:id="@+id/newBookBtn"
        android:layout_width="200dp"
        android:layout_height="100dp"
        android:layout_below="@id/titleWriting"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="25dp"
        android:layout_marginTop="20dp"
        android:background="@null"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        android:src="@drawable/butten_witing_a_new_book" />

    <!-- קו אפור בין כפתור הספר החדש לבין המשך כתיבה -->
    <View
        android:id="@+id/middleDivider"
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:layout_below="@id/newBookBtn"
        android:layout_marginTop="15dp"
        android:background="#D3D3D3" />

    <!-- כותרת המשך כתיבה -->
    <ImageView
        android:id="@+id/continueTitle"
        android:layout_width="110dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/middleDivider"
        android:layout_marginTop="15dp"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="20dp"
        android:src="@drawable/continue_writing_butten"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter" />

    <!-- אזור ספרים מהדאטה בייס (כרגע ריק) -->
    <FrameLayout
        android:id="@+id/booksPlaceholder"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_below="@id/continueTitle"
        android:layout_above="@id/navBar"
        android:layout_marginTop="15dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="כאן יוצגו ספרים מהדאטה בייס"
            android:textColor="#888888"
            android:textSize="18sp"/>
    </FrameLayout>

    <!-- סרגל ניווט תחתון -->
    <LinearLayout
        android:id="@+id/navBar"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:layout_alignParentBottom="true"
        android:orientation="horizontal"
        android:gravity="center"
        android:padding="8dp"
        android:background="#FFFFFF">

        <ImageButton
            android:id="@+id/libraryBtn"
            android:layout_width="0dp"
            android:layout_height="60dp"
            android:layout_weight="1"
            android:background="@null"
            android:src="@drawable/crators_library_butten"
            android:adjustViewBounds="true"
            android:scaleType="fitCenter" />

        <ImageButton
            android:id="@+id/writingBtn"
            android:layout_width="0dp"
            android:layout_height="60dp"
            android:layout_weight="1"
            android:background="@null"
            android:src="@drawable/wirting_book_butten"
            android:adjustViewBounds="true"
            android:scaleType="fitCenter" />

        <ImageButton
            android:id="@+id/booksBtn"
            android:layout_width="0dp"
            android:layout_height="60dp"
            android:layout_weight="1"
            android:background="@null"
            android:src="@drawable/book_butten1"
            android:adjustViewBounds="true"
            android:scaleType="fitCenter" />

    </LinearLayout>

</RelativeLayout>
